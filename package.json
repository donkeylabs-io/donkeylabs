{
  "name": "donkeylabs",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "test": "bun test",
    "build": "bun run --filter '*' build",
    "lint": "prettier --write . && eslint ."
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "typescript": "^5.9.3",
    "prettier": "^3.4.2",
    "eslint": "^9.17.0"
  }
}
