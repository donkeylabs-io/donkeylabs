{
  "name": "plugin",
  "type": "module",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@types/bun": "latest",
    "@types/prompts": "^2.4.9",
    "kysely-codegen": "^0.19.0",
    "picocolors": "^1.1.1",
    "prompts": "^2.4.2"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "kysely": "^0.28.9",
    "kysely-bun-sqlite": "^0.4.0",
    "zod": "^4.3.5"
  },
  "scripts": {
    "gen:types": "bun scripts/generate-types.ts",
    "gen:registry": "bun scripts/generate-registry.ts",
    "gen:client": "bun scripts/generate-client.ts",
    "plugin:new": "bun scripts/create-plugin.ts",
    "cli": "bun scripts/cli.ts",
    "test": "bun test"
  }
}